# PyPSA Dataset Optimizer Acceptance

Generated by `test/pypsa_dataset_optimizer_acceptance.jl`.

## Implementation Context
- JSON datasets are loaded with JSON.parsefile as PowerModels-style dictionaries.
- Multinetwork cases are identified by `multinetwork=true` and processed through `data["nw"]`; the active path receives explicit `:hour`, `:scenario`, and `:year` dimensions.
- Raw block fields are schema-checked as `n_block0`/`n_block_max`; solver copies map them to the formulation fields `n0`/`nmax`.
- Raw block-count invariants are hard-validated before solver-copy mapping: `0 <= na0 <= n_block0 <= n_block_max`.
- PyPSA `link` records are mapped to PowerModels `dcline` only when `carrier == "DC"`; non-DC link carriers are ignored.
- gSCR constraints are activated only through `ref_add_uc_gscr_block!` and `constraint_gscr_gershgorin_sufficient` in `build_uc_gscr_block_integration`.
- UC/scheduling mode is selected in the test adapter by fixing `nmax=n0`; CAPEXP mode allows `nmax>n0` where available.
- Results expose `n_block`, `na_block`, `su_block`, and `sd_block` through the active model variables and solution reporting hooks.

## Results

| dataset | test | snapshots | status | objective | min gSCR margin | near-binding | startup/shutdown | investment | warnings |
|---|---|---:|---|---:|---:|---:|---:|---:|---|
| base_s_5_3snap | load/schema | 3 | PASS | NaN | NaN | 0 | NaN | NaN |  |
| base_s_5_6snap | load/schema | 6 | PASS | NaN | NaN | 0 | NaN | NaN |  |
| base_s_5_24snap | load/schema | 24 | PASS | NaN | NaN | 0 | NaN | NaN |  |
| base_s_5_3snap | standard OPF | 3 | OPTIMAL | 0.000000 | NaN | 0 | NaN | NaN |  |
| base_s_5_3snap | passive metadata | 3 | OPTIMAL | 0.000000 | NaN | 0 | NaN | NaN | block metadata ignored by standard OPF |
| base_s_5_3snap | active UC/gSCR | 3 | INFEASIBLE | NaN | NaN | 0 | NaN | NaN | active UC/gSCR infeasible; reconstruction unavailable |
| base_s_5_3snap | active CAPEXP/gSCR | 3 | INFEASIBLE | NaN | NaN | 0 | NaN | NaN | active CAPEXP/gSCR infeasible; reconstruction unavailable |
| base_s_5_3snap | CAPEXP g_min sweep | 3 | low=INFEASIBLE, medium=INFEASIBLE, high=INFEASIBLE | NaN | NaN | 0 | NaN | NaN | monotone feasibility checked over low/medium/high g_min |
| base_s_5_6snap | active UC/gSCR | 6 | INFEASIBLE | NaN | NaN | 0 | NaN | NaN | active UC/gSCR infeasible; reconstruction unavailable |
| base_s_5_6snap | active CAPEXP/gSCR | 6 | INFEASIBLE | NaN | NaN | 0 | NaN | NaN | active CAPEXP/gSCR infeasible; reconstruction unavailable |
| base_s_5_6snap | CAPEXP g_min sweep | 6 | low=INFEASIBLE, medium=INFEASIBLE, high=INFEASIBLE | NaN | NaN | 0 | NaN | NaN | monotone feasibility checked over low/medium/high g_min |

## Warnings and Unresolved Issues

- The raw PyPSA JSON uses `n_block0`/`n_block_max`; the current optimizer formulation reads `n0`/`nmax`, so the tests apply a solver-copy adapter.
- `na0` invariants are validated on raw data for every block-enabled component and snapshot: `0 <= na0 <= n_block0 <= n_block_max`.
- PyPSA `link` entries with `carrier == "DC"` are mapped to PowerModels `dcline`; all other link carriers are ignored in solver copies.
- Standard OPF is run through `solve_mn_opf_strg` because the datasets include storage.
- Min-up/min-down, ramping, no-load costs, binary UC commitment variables, SDP/LMI constraints, and new gSCR formulations are not activated.
